apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "attendance-frontend.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "attendance-frontend.fullname" . }}
  port:
    targetPort: http
  host: {{ default nil .Values.route.host }}
  tls:
    termination: {{ if .Values.route.tls }}edge{{ else }}""{{ end }}