apiVersion: v1
kind: Service
metadata:
  name: {{ include "attendance-frontend.fullname" . }}
  labels:
    app: {{ include "attendance-frontend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "attendance-frontend.fullname" . }}